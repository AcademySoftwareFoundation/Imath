#include <OpenEXR/ImathColor.h>

int
main()
{
    Imath::C3c   r (255, 0, 0);
    Imath::C3c   g (0, 255, 0);
    Imath::C3c   b (0, 0, 255);
    
    Imath::C3c   w = (a + b + c) / 3;

    return 0;
}
